# 都更計票系統 - 環境變數配置範例
# Urban Renewal Voting System - Environment Configuration Example
#
# 使用方式：
#   1. 複製此檔案為 .env: cp .env.example .env
#   2. 根據環境需求修改對應的 Port 設定
#
# 兩種環境情境：
#   - Production (正式環境): 使用 docker-compose.prod.yml
#   - Development (開發環境): 使用 docker-compose.dev.yml

# ========================================
# Port 配置 (Port Configuration)
# ========================================
# 注意：請確保這些 Port 未被其他服務佔用

# 前端服務 Port (Frontend Port) - 僅 Production 使用
FRONTEND_PORT=4001

# 後端服務 Port (Backend Port)
BACKEND_PORT=4002

# 資料庫 Port (Database Port)
DB_PORT=4306

# phpMyAdmin Port
PHPMYADMIN_PORT=4003

# ========================================
# 後端 URL 配置 (Backend URL Configuration)
# ========================================
BACKEND_API_URL=http://localhost:4002/api
BACKEND_URL=http://localhost:4002

# ========================================
# 資料庫配置 (Database Configuration)
# ========================================
DB_HOST=mariadb
DB_DATABASE=urban_renewal
DB_USERNAME=root
DB_PASSWORD=urban_renewal_pass

# ========================================
# 環境說明 (Environment Notes)
# ========================================
# Production: 完整部署 Frontend + Backend + Database + phpMyAdmin + Cron
#   - 前端: http://localhost:${FRONTEND_PORT}
#   - 後端 API: http://localhost:${BACKEND_PORT}/api
#   - phpMyAdmin: http://localhost:${PHPMYADMIN_PORT}
#   - 資料庫: localhost:${DB_PORT}
#
# Development: 僅部署 Backend + Database + phpMyAdmin + Cron
#   - 前端: 使用 npm run dev 在本地執行 (通常是 http://localhost:3000)
#   - 後端 API: http://localhost:${BACKEND_PORT}/api
#   - phpMyAdmin: http://localhost:${PHPMYADMIN_PORT}
#   - 資料庫: localhost:${DB_PORT}
