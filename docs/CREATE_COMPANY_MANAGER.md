# 企業管理者帳號建立工具

快速建立企業管理者測試帳號的便利腳本。

## 使用方式

```bash
./create-company-manager.sh [USERNAME] [PASSWORD] [EMAIL] [FULL_NAME]
```

## 功能特色

- ✅ 快速建立企業管理者帳號
- ✅ 支援自訂帳號、密碼、Email 和姓名
- ✅ **智慧隨機產生** - 只提供帳號密碼時,自動產生隨機 Email 和名字
- ✅ 自動檢查帳號和 Email 是否重複
- ✅ 建立後顯示完整的登入資訊
- ✅ 彩色輸出,清楚易讀

## 使用範例

### 1. 使用預設值建立

```bash
./create-company-manager.sh
```

預設值:
- 帳號: `company_manager`
- 密碼: `manager123`
- Email: `manager@company.com`
- 姓名: `企業管理者`

### 2. 自訂帳號和密碼 (自動隨機產生 Email 和名字)

```bash
./create-company-manager.sh testmanager pass123
```

這會使用:
- 帳號: `testmanager`
- 密碼: `pass123`
- Email: `testmanager@隨機8碼.com` (自動隨機產生)
- 姓名: `testmanager_管理者` (自動產生)

**注意**: 當只提供帳號和密碼時,系統會自動產生唯一的隨機 Email (格式: `帳號@隨機碼.com`) 和名字 (格式: `帳號_管理者`),避免 Email 重複問題。

### 3. 完整自訂所有欄位

```bash
./create-company-manager.sh john john123 john@test.com "John Doe"
```

這會使用:
- 帳號: `john`
- 密碼: `john123`
- Email: `john@test.com`
- 姓名: `John Doe`

### 4. 查看幫助訊息

```bash
./create-company-manager.sh --help
```

或

```bash
./create-company-manager.sh -h
```

## 輸出範例

成功建立後會顯示:

```
✅ 企業管理者帳號建立成功!

📊 帳號詳細資訊:
   使用者 ID: 5
   帳號: testmanager
   密碼: test123
   Email: test@company.com
   姓名: 測試管理者
   角色: admin
   使用者類型: enterprise
   企業管理者: 是

🔗 登入資訊:
   前端登入頁面: http://localhost:3000/login (或 http://localhost:4001/login)
   API 端點: http://localhost:9228/api/auth/login

✨ 現在可以使用此帳號登入系統!
```

## 帳號設定

建立的帳號會有以下屬性:

- **角色 (role)**: `admin`
- **使用者類型 (user_type)**: `enterprise` (企業使用者)
- **企業管理者 (is_company_manager)**: `是` (1)
- **啟用狀態 (is_active)**: `啟用` (1)

## 錯誤處理

### 後端服務未運行

```
❌ 錯誤: 後端服務未運行
請先執行: ./start-dev.sh
```

### 帳號或 Email 已存在

```
❌ 錯誤: 帳號或 Email 已存在!
   帳號 'testmanager' 已被使用
```

## 注意事項

1. 執行前請確保後端服務已啟動 (`./start-dev.sh`)
2. 此腳本僅供測試環境使用
3. 密碼會以 PHP 的 `PASSWORD_DEFAULT` 演算法加密
4. 帳號和 Email 必須是唯一的

## 技術細節

- 使用 Docker 容器中的 MariaDB 客戶端
- 自動讀取 `.env` 檔案的資料庫連線資訊
- 使用 PHP 的 `password_hash()` 函數來加密密碼
- 支援中文姓名和特殊字元
- 使用 `date +%s%N | md5sum` 產生隨機 Email,確保唯一性
