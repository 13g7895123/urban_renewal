# 都更計票系統 - 後端環境變數配置範例
# Urban Renewal Voting System - Backend Environment Configuration Example

# 應用程式環境 (Application Environment)
# 可選值: development, production, testing
CI_ENVIRONMENT = development

#--------------------------------------------------------------------
# 資料庫設定 (DATABASE CONFIGURATION)
#--------------------------------------------------------------------
database.default.hostname = mariadb
database.default.database = urban_renewal
database.default.username = root
database.default.password = urban_renewal_pass
database.default.DBDriver = MySQLi
database.default.DBPrefix =
database.default.port = 3306

#--------------------------------------------------------------------
# JWT 設定 (JWT CONFIGURATION)
#--------------------------------------------------------------------
# JWT 密鑰 (請在生產環境中更改此值)
# jwt.secretKey = your_secret_key_here
# JWT Token 有效期限 (秒)
# jwt.expiresIn = 3600

#--------------------------------------------------------------------
# CORS 設定 (CORS CONFIGURATION)
#--------------------------------------------------------------------
# 允許的來源 (Allowed Origins)
# cors.allowedOrigins = http://localhost:4001,http://localhost:3000

#--------------------------------------------------------------------
# 檔案上傳設定 (FILE UPLOAD CONFIGURATION)
#--------------------------------------------------------------------
# 最大檔案大小 (MB)
# upload.maxSize = 10
# 允許的檔案類型
# upload.allowedTypes = pdf,doc,docx,xls,xlsx,jpg,jpeg,png
